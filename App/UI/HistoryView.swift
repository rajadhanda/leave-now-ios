import SwiftUI

struct HistoryView: View {
    var body: some View {
        Text("History coming soon")
            .font(.subheadline)
            .foregroundStyle(.secondary)
            .navigationTitle("History")
    }
}
